#Задание 1
#Просмотреть содержимое директорий /etc, /proc, /home. Посмотреть пару произвольных файлов в /etc.
#Просмотреть содержимое директорий /etc, /proc, /home. 
 ls /etc
 ls /proc
 ls /home
#Посмотреть пару произвольных файлов в /etc
cat /etc/hosts
cat /etc/resolv.conf



#Задание 2
#Выяснить, для чего предназначена команда cat. Используя данную команду, создать два файла с данными, а затем объединить их. Просмотреть содержимое созданного файла. Переименовать файл, дав ему новое имя.
#спользуя cat, создать два файла с данными
cat > /home/file1 << EOF
Cтрока 1
EOF
cat > /home/file2 << EOF
Cтрока 2
EOF
#объединить, переименовать файл, дав ему новое имя.
cat /home/file1  /home/file2 > /home/file3
#Просмотреть содержимое созданного файла.
cat file3



#Задание 3
#Создать несколько файлов. Создать директорию, переместить файл туда. Удалить все созданные в этом и предыдущем задании директории и файлы.
#Создать несколько файлов
touch  /home/file1_touch 
touch  /home/file2_touch 
touch  /home/file3_touch 
#Создать директорию
mkdir /home/dir_torch
#переместить файл в директорию
mv /home/file*_touch /home/dir_torch
#Удалить все созданные в этом и предыдущем задании директории и файлы
rm -rf /home/file* 
rm -rf /home/dir_torch 



#Задание 4
#В ОС Linux скрытыми файлами считаются те, имена которых начинаются с символа “.”. Сколько скрытых файлов в вашем домашнем каталоге? (Использовать конвейер. Подсказка: для подсчета количества строк можно использовать wc.)
#Сколько скрытых файлов в вашем домашнем каталоге?
ls -a /home | grep "^\." | wc -l



#Задание 5
#Попробовать вывести с помощью cat все файлы в директории /etc. Направить ошибки в отдельный файл в вашей домашней директории. Сколько файлов, которые не удалось посмотреть, #оказалось в списке?
cat /etc/* 2> /home/file.err 
cat /home/file.err | wc -l



#Задание 6
#Запустить в одном терминале программу, а в другом терминале посмотреть PID процесса и остановить с помощью kill, посылая разные типы сигналов. Что происходит?
#Все работает



#Задание 7
#Отобразить в /dev список устройств, которые в настоящее время используются вашим UID (для этого используется команда lsof). Организовать конвейер через less, чтобы посмотреть их должным образом. 
UserName=$(id -u -n)
lsof /dev |  less | grep "$UserName" 



#Задание 8
#Ничего сложного, нехватает времени



#Задание 9
#Полезное задание на конвейер. Использовать команду cut на вывод длинного списка каталога, чтобы отобразить только права доступа к файлам. 
#Затем отправить в конвейере этот вывод на #sort и uniq, чтобы отфильтровать все повторяющиеся строки. 
#Потом с помощью wc подсчитать различные типы разрешений в этом каталоге. 
ls -al /etc | cut -d " " -f1 |grep -v 'total' | sort | uniq |wc -l
#Самостоятельно решить задачу, как сделать так, чтобы в подсчет не добавлялись строка «Итого» и файлы . и .. (ссылки на текущую и родительскую директории).
ls -l /etc |grep -v 'total' | cut -d " " -f1 | sort | grep -v '^lr*' | uniq | wc -l
