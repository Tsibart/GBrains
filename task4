#Задание 1
#скрипт, который удаляет из текстового файла пустые строки и заменяет маленькие символы на большие 
#сохраняем файл с именем DElines.sh и делаем его исполняемым
#вводим полный путь к файлу, например ./DElines.sh /opt/file.txt
#!/bin/bash
file=$1
tr '[:upper:]' '[:lower:]' < $file >> /tmp/tmpfile.txt          #замена символов на маленькие 
rm -rf $file
sed -i '/^$/d' /tmp/tmpfile.txt | mv /tmp/tmpfile.txt $file     #Удаляем пустые строки



#Задание 2
#создать  директории для нескольких годов (2010 — 2017),
#в них — поддиректории для месяцев (от 01 до 12), и 
#в каждый из них несколько файлов XXX.txt, содержащий текст "Файл 001"
#!/bin/bash
mkdir /home/folder;
rm -rf /home/folder/*;
mkdir -p /home/folder/20{10..17}/{01..12};
for a in {2010..2017};do for j in {01..12};do touch /home/folder/$a/$j/0$j.txt;echo "Файл 0$j">/home/folder/$a/$j/0$j.txt;done;done

#Задание 3
#Создать файл crontab, который ежедневно регистрирует занятое каждым пользователем дисковое пространство в его домашней директории.
#создаем файл /home/SizeFolder.sh и делаем его исполняемым
#!/bin/bash
du -sh /home/* >/home/LogSizeFolder.txt


#запускаем следующую команду в консоле
# Пример запуска скрипта в 6 утра каждый день:
#crontab -e
#Вставляем
0 6 * * * /home/SizeFolder.sh
#Проверяем
crontab -l
